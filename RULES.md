# 项目规则

## 领域规则

1. 每个人物必须有唯一 ID、非空姓名和性别。
2. 初版性别仅支持“男”和“女”；代码值分别为 `male`、`female`。
3. 初版关系仅支持：父亲、母亲、丈夫、妻子、儿子、女儿、哥哥、姐姐、弟弟、妹妹、同事。
4. 每条关系必须连接两个已存在的人物，且不能连接人物自身。
5. 同一来源人物、目标人物与关系类型的组合不得重复。
6. 删除人物时必须明确提示其关联关系也会被删除，并在确认后级联删除。
7. 修改关系时必须继续满足人物存在、自关联和重复关系校验。
8. 创建关系时必须自动创建语义对应的反向关系；编辑或删除任意一条时同步维护其反向关系。
   删除关系无需确认弹窗，点击即删除该关系及其反向关系，并清理对应选中状态；不删除人物或其他关系。删除人物仍遵循第 6 条的确认要求。
9. 新增亲子、兄弟姐妹或夫妻关系时，默认启用智能补全：根据已知父母及其唯一配偶补齐亲子关系，根据共同父母发现兄弟姐妹。新增夫妻关系时检查双方已有的直接子女，仅在双方互为唯一配偶时补齐另一方为家长；逐个子女保留已有父母，不把其他家长传播到别的子女，不递归补齐孙辈。全部关系确定且无冲突时直接保存，不弹出确认窗口；仅长幼未知或存在冲突时预览确认，长幼必须由用户选择或明确跳过。取消不保存任何关系，手动关系及补全项连同反向关系一次性保存。
10. 智能补全不覆盖已有关系；多配偶、父母候选冲突或亲属身份冲突时提示并跳过。可关闭智能补全，仅保存手动关系及其反向关系。编辑和删除不级联推导其他家庭关系。
11. 新增、编辑及批量保存关系时，称谓须与来源人物性别一致；同事不受限。当前丈夫/妻子模型同时要求反向称谓与目标人物性别一致，即丈夫为男→女，妻子为女→男。弹窗选项过滤与保存校验共用领域规则；不自动清理既有数据。

## 工程规则

1. TypeScript 开启严格模式，不以 `any` 绕过领域类型。
2. 页面组件不直接实现持久化细节；数据访问通过独立的数据层或状态层完成。
3. 领域常量和类型保持单一来源，不在组件内重复声明。
4. 新功能需覆盖核心成功路径与关键校验路径；测试框架在实现业务功能阶段选定。
5. 不提交 `node_modules/`、构建产物、编辑器配置或本地密钥。
6. 不在前端代码中存放密钥、口令或其他敏感信息。
7. 未在需求中确认前，不擅自增加关系类型或性别类型；智能补全仅限已确认的父母、子女、兄弟姐妹规则，不推导祖孙、叔侄等其他亲属关系。
8. CloudKit Web API Token 虽需发布到浏览器，仍必须通过构建环境注入并在 CloudKit Console 限制 Allowed Origins；Apple 私钥、server-to-server key 和用户令牌不得写入仓库或前端环境变量。
