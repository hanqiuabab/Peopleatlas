# 人际关系网络网站需求

## 1. 产品目标

提供一个以神经网络/关系图谱形式展示人际关系的网站，使用户能够直观查看人物节点及人物之间的关系，并维护人物和关系数据。

## 2. 初版范围

### 2.1 人物管理

- 新增人物。
- 修改人物信息。
- 删除人物。
- 人物字段：
  - 姓名：必填文本。
  - 性别：必填，初版支持男、女。

### 2.2 关系管理

- 在两个已存在人物之间新增关系。
- 修改已有关系。
- 删除已有关系：点击删除立即移除该关系及其反向关系，不弹出确认窗口；同步更新图谱、列表及对应选中状态。人物删除仍需确认。
- 创建关系时自动创建对应的反向关系，编辑或删除任一方向时同步维护另一方向。
- 初版支持的关系：父亲、母亲、丈夫、妻子、儿子、女儿、哥哥、姐姐、弟弟、妹妹、同事。
- 新增、编辑关系弹窗（含图谱拖拽入口）按“A 是 B 的某种关系”及双方性别过滤选项：A 为男性时提供父亲、儿子、哥哥、弟弟，A 为女性时提供母亲、女儿、姐姐、妹妹；同事不受性别限制。按当前丈夫/妻子互为反向的模型，仅 A 男/B 女时提供丈夫，A 女/B 男时提供妻子。
- 切换 A 或 B 后即时更新选项；原选择仍适用则保留，否则清空并提示重新选择，不自动替换成另一种关系。缺少人物或两端为同一人时不提供关系选项。提交时复用同一性别校验，防止普通添加、编辑和智能补全保存不匹配的关系；既有关系不会因筛选而被批量修改或删除。

#### 智能添加关系

- 新增关系默认开启“智能补齐家庭关系”，可关闭；所有显示模式和图谱拖拽建关系入口共用该流程。
- 新增亲子关系时，按“已知父母的唯一配偶为另一位家长”的假设补齐亲子关系，并发现与已有子女之间缺失的兄弟姐妹关系。
- 新增夫妻关系时，同样检查双方已有的直接子女，双方互为唯一配偶且无冲突时补齐另一方的亲子及反向关系，使先添加亲子或先添加夫妻在无冲突的直接家庭场景中得到一致结果。逐个子女检查已有父母，不将某个子女的其他家长传播到别的子女，不递归补齐孙辈；任一配偶有多位配偶时提示并跳过本次家庭补全。补齐后发现兄弟姐妹时仍需确认未知长幼。关闭智能补全时仅保存夫妻双向关系；不自动回填历史数据。
- 新增兄弟姐妹关系时，共享双方已知父母，并补齐这些父母的唯一配偶作为家长的关系；仅围绕本次涉及的子女发现直接家庭关系，不递归推导远亲。
- 自动关系名称根据人物性别生成。长幼未知时必须弹窗，由用户为每一对选择哥哥/弟弟或姐姐/妹妹，也可以明确跳过，不默认猜测年龄。
- 所有补全关系都能确定且无冲突时，不弹出确认窗口，直接一次性保存原始关系、自动补全项及对应反向关系。仅长幼未知或存在冲突时展示原始关系、自动补全项、待确认项及推导原因，确认后一次性保存；返回修改保留表单，取消完全不写入。
- 已有关系不重复补入；多配偶、同一性别的不同父母候选、祖先循环或亲属身份冲突不擅自处理，提示跳过并保留原数据。重组家庭等不符合默认假设的情况可在提交前关闭智能补全。
- 编辑或删除仅维护原有反向关系对，不自动修改此前补齐的其他家庭关系。

### 2.3 图谱展示

- 支持“平面”“星球”和“星云”三种显示风格，并可在页面顶部即时切换；三种视图共用同一套人物、关系、筛选和选中状态。
- 页面默认显示平面模式；首次打开、没有有效排列偏好时默认采用圆环排列并自动旋转，无需点击开启。记住用户选择的平面排列方式，刷新页面或切换到星球/星云后返回时恢复；本地存储不可用时，仍在当前页面会话内保留选择。仍保留手动暂停、选择暂停、少于两人不旋转和减少动态效果设置，以及切换等级或自由排列的能力。
- 人物以节点呈现，关系以连接节点的边呈现。
- 所有模式按真实关系优化人物距离：夫妻优先靠近，其次兄弟姐妹、父母子女，同事联系为较弱参考。正反关系只计一次，不新增业务关系；复杂家庭中按优先级尽量靠近，不保证所有成员同时相邻。
- 圆环按家庭关系排序后均匀排列；等级排列保持辈分层级，并优化同层相邻顺序；星球与星云在保持各自形态的前提下拉近亲近人物并避免节点挤在一起。新增、修改、删除关系后自动重新计算，暂停动画时也更新。仅切换关系线筛选不应改变亲疏依据。
- 自由排列的默认位置和复位采用关系布局；手动拖过的人物位置在关系变更时保留，其余人物自动调整。
- 平面视图节点展示人物姓名；星球视图节点在鼠标悬停或选中后展示姓名，并可区分性别；星球人物的选中状态使用仅环绕球体的圆形外轮廓，不包围姓名标签。
- 平面视图的边展示关系名称；星球视图默认隐藏关系名称，仅选中关系线后显示名称；两种视图的关系线均不显示端点箭头，并通过关系文字表达方向语义，“同事”按对等关系展示；关系选中状态不使用方形外围轮廓。
- 一对自动生成的正反关系在图谱中合并为一条双向语义线，例如“父亲 ↔ 女儿”；列表和数据层仍保留两个方向供筛选与编辑。
- 支持基本的画布缩放、拖拽和平移。
- 平面、星球和星云模式的右下角均提供全屏按钮；全屏范围包含完整图谱画布、筛选器、选中信息和控制按钮，可通过按钮或 Esc 退出。
- 平面视图右下角提供“按等级排列”按钮。父亲、母亲相对目标人物等级加一，儿子、女儿等级减一；丈夫、妻子、兄弟姐妹和同事保持同级。启用时人物按等级从上到下分层排列，并可恢复自由排列；新增、修改或删除关系后必须立即重新计算等级并更新排列。
- 等级排列中，点击或拖动人物不退出自动布局，连接句柄仍可拖拽新建关系；“复位”仅重置视角，不改变排列方式。需要手动移动人物时，先点击“恢复自由排列”，退出时保留当前人物位置。人物和关系增删改、智能补全均不得隐式切换排列方式。
- 等级排列中，没有任何有效关系的人物固定为等级 -1，位于有关联人物的下方；有关联分组最低等级仍为 0。新增关系后自动进入对应等级，删除最后一条关系后自动回到 -1。以完整关系数据判断是否孤立，隐藏连线或筛选关系类型不改变该判断。
- 平面视图右下角提供“按圆环排列”，与等级排列互斥。可见人物均匀分布在圆周上，新增、删除人物或切换性别筛选时自动重排，即使暂停旋转也应更新。圆环默认缓慢自动旋转，支持暂停/继续，人物名称保持正向，连线跟随节点。选中人物或关系以及拖动画布/连接句柄时暂停；再次点击同一目标或点击空白取消选择后恢复自动旋转，但保留用户的手动暂停设置。单个人物居中显示，少于两人时不旋转；遵循系统减少动态效果设置。退出圆环后可自由拖动人物节点。
- 选中人物节点后显示连接句柄；可从句柄拖到另一个人物节点，松手后打开已预填两端人物的关系绑定弹窗。
- 选中人物后，右侧关系记录仅显示该人物作为起点或终点参与的关系；星球视图同时仅显示与该人物有关的关系线；取消选择后恢复全部关系记录和关系线。
- 空数据时展示明确的空状态及新增人物入口。
- 星球视图采用深色沉浸式界面，人物按球面空间分布，并提供景深、发光粒子、轨道环、自动旋转、暂停、横向拖拽旋转、滚轮缩放和重置视角；选中人物或关系时自动暂停旋转，再次点击同一人物或关系时取消选择并自动恢复旋转。
- 星云视图采用自由分布的深色宇宙关系网络：关系度高的人物靠近视觉核心并使用更大的发光节点，关系以多层细丝呈现，背景包含不代表业务数据的环境星粒和光晕；支持自动缓慢漂移、暂停、拖拽探索、滚轮缩放和重置视角。选中人物时仅显示其相关关系丝线，选中人物或关系时暂停，再次点击取消选择。
- 平面、星球和星云视图右上角均提供“男性”“女性”“关系”显示开关，并列出全部关系类型供用户逐项显示或隐藏；三种视图共享筛选状态，隐藏人物时同步隐藏其关联关系。一级“关系”关闭时同步关闭全部关系类型，打开时同步开启全部关系类型；一级关闭后仍可单独配置二级关系类型，开启任一二级类型时应自动重新启用关系层并立即显示该类型的连线。

### 2.4 Apple 账户与 iCloud 同步

- 网页通过 Apple CloudKit JS 提供 Apple 账户登录和退出，不自建口令系统，不在前端保存 Apple 私钥或口令。
- 未登录、离线或 iCloud 暂时不可用时继续使用当前浏览器的 `localStorage`；登录后将图谱同步到该 Apple 账户的私有 CloudKit 数据库。
- 界面明确展示未配置、未登录、正在同步、已同步、离线/失败和冲突状态；不将“账户可用”误报为每项更改均已上传。
- 首次登录或多端同时修改造成本地与云端分歧时，不静默覆盖；由用户选择合并两份数据、保留本地或使用云端。合并时同 ID 记录以 `updatedAt` 较新者为准，并继续执行引用完整性清理。
- 页面恢复前台、网络恢复及定时检查时拉取云端新版本；本地修改先落盘，再后台同步。
- CloudKit 容器、环境和 Web API Token 由构建环境配置。API Token 必须在 CloudKit Console 限制允许的网站来源，正式上线前需将记录类型部署到 Production。
- 网页使用独立的 CloudKit 网络快照记录；与 `PeopleAtlas/` 原生 SwiftData 记录的互通不属于本轮范围。

## 3. 数据模型

### 人物 Person

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 唯一标识 |
| name | string | 姓名，必填 |
| gender | male \| female | 性别，必填 |
| createdAt | string | 创建时间 |
| updatedAt | string | 更新时间 |

### 关系 Relationship

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | string | 唯一标识 |
| sourcePersonId | string | 来源人物 ID |
| targetPersonId | string | 目标人物 ID |
| type | RelationshipType | 关系类型 |
| createdAt | string | 创建时间 |
| updatedAt | string | 更新时间 |

## 4. 业务约束

- 以 `RULES.md` 中的领域规则为准。
- 自动补全直接反向关系。例如添加“A 是 B 的儿子”，若 B 为男性则同时生成“B 是 A 的父亲”，若 B 为女性则生成“B 是 A 的母亲”。不继续推导祖孙等间接关系。
- 不支持多用户协作、细粒度权限管理和人与人之间的数据共享。
- 未登录时采用浏览器 `localStorage` 持久化；登录 Apple 账户后使用私有 CloudKit 数据库进行跨浏览器同步，并保留本地离线副本。

## 5. 验收要点

- 可以完成对人物和关系的新增、查看、修改、删除。
- 刷新页面后数据按最终选定的持久化方案保留。
- 无效或重复操作有可理解的提示。
- 删除人物不会留下指向不存在人物的孤立关系。
- 桌面端和常见移动端宽度下均可操作。

## 6. 待确认事项

- 是否需要支持“未知/其他”性别。
- 是否需要导入、导出、搜索和筛选。
- 是否需要多个独立关系图谱。
- 后续是否将网页 CloudKit 快照与原生 SwiftData 记录打通。
